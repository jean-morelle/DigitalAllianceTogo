namespace DigitalAllianceTogo.Domain.Enum
{
    public enum ModePaiement
    {
        Integre = 1,
        Externe = 2,
        Avoir = 3 // réglé avec un avoir du client : confirmé immédiatement, sans vérification externe
    }
}
